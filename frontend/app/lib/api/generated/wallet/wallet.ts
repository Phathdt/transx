/**
 * Generated by orval v8.22.0 🍺
 * Do not edit manually.
 * transx API
 * transx API
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateTransferBody,
  DtoAccountListResponse,
  DtoAccountLookupResponse,
  DtoAccountResponse,
  DtoCreateAccountCommand,
  DtoTransferListResponse,
  DtoTransferResponse,
  ListAccountsParams,
  ListTransfersParams
} from '../models';

import { serverApiClient } from '../../server-http-mutator.ts';

export const getListAccountsUrl = (params?: ListAccountsParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/accounts?${stringifiedParams}` : `/accounts`
}

/**
 * List the caller's wallet accounts, paginated, with optional currency and status filters
 * @summary List the caller's wallet accounts, paginated, with optional currency and status filters
 */
export const listAccounts = async (params?: ListAccountsParams, options?: RequestInit): Promise<DtoAccountListResponse> => {

  return serverApiClient<DtoAccountListResponse>(getListAccountsUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getCreateAccountUrl = () => {




  return `/accounts`
}

/**
 * Create a wallet account for the caller
 * @summary Create a wallet account for the caller
 */
export const createAccount = async (dtoCreateAccountCommand: DtoCreateAccountCommand, options?: RequestInit): Promise<DtoAccountResponse> => {

  return serverApiClient<DtoAccountResponse>(getCreateAccountUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(dtoCreateAccountCommand)
  }
);}


export const getGetAccountUrl = (accountRef: string,) => {




  return `/accounts/${accountRef}`
}

/**
 * Get a wallet account balance by its accountRef (ACC- + ULID)
 * @summary Get a wallet account balance by its accountRef (ACC- + ULID)
 */
export const getAccount = async (accountRef: string, options?: RequestInit): Promise<DtoAccountResponse> => {

  return serverApiClient<DtoAccountResponse>(getGetAccountUrl(accountRef),
  {
    ...options,
    method: 'GET'


  }
);}


export const getLookupAccountUrl = (accountType: string,
    accountRef: string,) => {




  return `/accounts/${accountType}/${accountRef}`
}

/**
 * Look up an internal or external account for transfer beneficiary validation
 * @summary Look up an internal or external account for transfer beneficiary validation
 */
export const lookupAccount = async (accountType: string,
    accountRef: string, options?: RequestInit): Promise<DtoAccountLookupResponse> => {

  return serverApiClient<DtoAccountLookupResponse>(getLookupAccountUrl(accountType,accountRef),
  {
    ...options,
    method: 'GET'


  }
);}


export const getListTransfersUrl = (params?: ListTransfersParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/transfers?${stringifiedParams}` : `/transfers`
}

/**
 * List the caller's transfers, paginated, with optional status and accountRef filters
 * @summary List the caller's transfers, paginated, with optional status and accountRef filters
 */
export const listTransfers = async (params?: ListTransfersParams, options?: RequestInit): Promise<DtoTransferListResponse> => {

  return serverApiClient<DtoTransferListResponse>(getListTransfersUrl(params),
  {
    ...options,
    method: 'GET'


  }
);}


export const getCreateTransferUrl = () => {




  return `/transfers`
}

/**
 * Create a transfer (idempotent via Idempotency-Key). INTERNAL needs toAccountRef (an ACC- account ref); EXTERNAL omits it or carries a free-text beneficiary id (provider is set from server config).
 * @summary Create a transfer (idempotent via Idempotency-Key). INTERNAL needs toAccountRef (an ACC- account ref); EXTERNAL omits it or carries a free-text beneficiary id (provider is set from server config).
 */
export const createTransfer = async (createTransferBody: CreateTransferBody, options?: RequestInit): Promise<DtoTransferResponse> => {

  return serverApiClient<DtoTransferResponse>(getCreateTransferUrl(),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(createTransferBody)
  }
);}


export const getGetTransferUrl = (transferId: string,) => {




  return `/transfers/${transferId}`
}

/**
 * Get a transfer by its business reference (ETN- for EXTERNAL, ITN- for INTERNAL, followed by a ULID)
 * @summary Get a transfer by its business reference (ETN- for EXTERNAL, ITN- for INTERNAL, followed by a ULID)
 */
export const getTransfer = async (transferId: string, options?: RequestInit): Promise<DtoTransferResponse> => {

  return serverApiClient<DtoTransferResponse>(getGetTransferUrl(transferId),
  {
    ...options,
    method: 'GET'


  }
);}


export const getCancelTransferUrl = (transferId: string,) => {




  return `/transfers/${transferId}/cancel`
}

/**
 * Cancel a SCHEDULED transfer before its execute time; idempotent if already CANCELLED
 * @summary Cancel a SCHEDULED transfer before its execute time; idempotent if already CANCELLED
 */
export const cancelTransfer = async (transferId: string, options?: RequestInit): Promise<DtoTransferResponse> => {

  return serverApiClient<DtoTransferResponse>(getCancelTransferUrl(transferId),
  {
    ...options,
    method: 'POST'


  }
);}


