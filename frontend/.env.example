# Backend gateway base URL (Traefik) for the dev proxy target.
VITE_API_PROXY_TARGET=http://localhost:4000

# Optional: override the API base path the browser calls. Defaults to /api/v1.
# VITE_API_BASE_URL=/api/v1
